{
  "name": "@hubbard/e2e-tests",
  "version": "0.2.1",
  "main": "index.js",
  "license": "Apache-2.0",
  "devDependencies": {
    "cypress": "^5.3.0"
  },
  "scripts": {
    "test": "yarn run cypress run --headless --record --key null",
    "test-local": "yarn run cypress open"
  }
}
